<% @pages = Spina::Page.order(id: :desc).page(params[:page]).per(10) %>
<% @pages.each do |page| %>
    <% next if page.materialized_path.nil? %>
    <%= link_to page.title, page.materialized_path %><br>
<% end %>
<%= paginate @pages %>
