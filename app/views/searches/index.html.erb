<p style="color: green"><%= notice %></p>

<h1>Searches</h1>

<%= form_with url: '/searches', method: 'post', class: 'turbo-form' do |f| %>
  <%= f.label :name %>
  <%= f.text_field :q %>

  <%= f.submit 'Submit' %>
<% end %>

<div id="searches">
</div>